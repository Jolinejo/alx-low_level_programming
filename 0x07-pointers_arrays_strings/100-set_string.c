#include "main.h"

/**
 * set_string - Entry point
 * @s: source striing
 * @to: dest string
 * Description: to check if letter is uppercase
 * Return: no return
 */
char *set_string(char **s, char *to)
{
	*s = to;
	return (*s);
}
